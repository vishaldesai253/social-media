name: Matrix Parallel Jobs

on:
  workflow_dispatch:

jobs:
  tests:
    runs-on: macos-15
    strategy:
      matrix:
        shard: [1, 2, 3, 4]
    steps:
      - name: Run shard ${{ matrix.shard }}
        run: |
          echo "Running shard ${{ matrix.shard }}"
          sleep 10
